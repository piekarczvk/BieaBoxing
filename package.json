{
  "name": "biea-boxing",
  "version": "1.0.0",
  "private": true,
  "description": "Biea Boxing Club official website",
  "scripts": {
    "dev": "python3 -m http.server 8080",
    "build": "bash scripts/build.sh",
    "deploy": "npm run build && wrangler versions upload"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "terser": "^5.39.0",
    "wrangler": "^4.0.0"
  }
}
